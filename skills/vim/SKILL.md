# Vim Skill

When writing Vim/Neovim configuration:
- Use Lua over Vimscript when possible
- Lazy load plugins for faster startup
- Use which-key compatible descriptions
- Follow the leader key convention
- Group related mappings logically
- Prefer built-in LSP over external tools
