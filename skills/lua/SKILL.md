# Lua Skill

When writing Lua code:
- Use local variables by default
- Prefer single quotes for strings
- Use vim.api for Neovim API calls
- Prefer vim.keymap.set over vim.api.nvim_set_keymap
- Use vim.fn for Vimscript functions
- Check for nil before accessing table fields
- Use metatables for OOP patterns
