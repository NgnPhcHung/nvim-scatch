require('mini.animate').setup({
  scroll = {
    enable = true,
    duration = 200,
    easing = 'linear',
  },
  cursor = {
    enable = true,
    duration = 100,
    easing = 'cubic_in_out',
  },
  -- Thêm hiệu ứng cho cửa sổ chia
  split = {
    enable = true,
    duration = 150,
    easing = 'ease_in_out',
  },
})

